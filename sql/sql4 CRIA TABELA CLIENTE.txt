CREATE TABLE CLIENTE (
    CODIGO INTEGER NOT NULL,
    NOME VARCHAR(100) CHARACTER SET WIN1252,
    ENDERECO VARCHAR(100) CHARACTER SET WIN1252,
    CIDADE VARCHAR(100) CHARACTER SET WIN1252,
    BAIRRO VARCHAR(50) CHARACTER SET WIN1252,
    UF VARCHAR(2) CHARACTER SET WIN1252,
    "CPF / CNPJ" VARCHAR(20) CHARACTER SET WIN1252);

alter table CLIENTE
add constraint PK_CLIENTE
primary key (CODIGO);